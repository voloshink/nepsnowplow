node_modules
out
dist
jre
test-results
playwright-report
# The new TypeScript renderer/main/preload code is type-checked via `yarn
# typecheck`. ESLint will be re-wired with @typescript-eslint when the
# legacy JS under `app/` and `server/` is removed in Phase 5.
src
